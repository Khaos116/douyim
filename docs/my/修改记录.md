# 改造修改记录

my_dev 分支改造全程记录，按时间从上往下追加，历史条目不覆盖、不删除。
Phase 条目格式见 `AGENTS.md` §3。

## 基建：打包签名 / APK 归档 / 混淆对齐 Sesame-M（2026-09-23）

- commit：`build: 对齐 Sesame-M 签名/归档/混淆，密钥公开进仓库`
- 修改文件：
  - `app/build.gradle.kts`（修改）：版本号集中为 `appVersionName`；`keystore.properties` 条件签名（有则 release 密钥 v1+v2，无则回退 debug 签名并打印提示）；release 产物固定命名 `douxianren-lsp-api102-v<version>.apk`；`assembleRelease` 后自动归档到根目录 `APK/Release/<base>_yyyyMMdd_HHmmss.apk`（Kotlin DSL 下 `configureEach` 无隐式 `it`，改用 `afterEvaluate + forEach`；AGP 9 用 `enableV1Signing/enableV2Signing`）
  - `app/proguard-rules.pro`（修改）：对齐 Sesame-M——Xposed 入口全 keep（去掉 `allowobfuscation`），`hook.**` 由 `keepclassmembers` 改为整包 `-keep`，新增 `SourceFile/LineNumberTable` 保留与 libxposed `dontwarn`
  - `.gitignore`（修改）：新增 `APK/`，并注明密钥公开进仓库
  - `xqe.jks`（新增，进 Git）：从 Sesame-M 直接复制（名字不改），与 Sesame-M 同一签名身份，SHA-256 `46:CC:A3:8D:A5:88:40:96:67:C9:FD:6B:89:58:C8:DB:1E:7D:CE:4F:01:AB:10:AC:9B:64:8E:BC:62:E7:12:E2`
  - `keystore.properties`（新增，进 Git）：从 Sesame-M 直接复制（名字不改）
- 核心逻辑：签名/归档/混淆三处照搬 Sesame-M（Groovy 改写为 Kotlin DSL，密钥文件直接复制）；未照搬 ABI 拆分（本模块纯 Java，无 native 库）；与 Sesame-M 两点不同：①密钥公开进仓库（用户明确要求）；②无 keystore 时回退 debug 签名而非产出未签名包。`dist/1.5.1` 系他机 debug 签名（SHA-256 `B6C87A19…`），与本机 debug 密钥不一致，无法沿用
- 风险点：新密钥与历史发布件签名身份不同，老用户覆盖安装失败，需卸载重装——发版时必须明确通知；hook 包整包 keep 后混淆强度下降（换稳定性）
- 真机验证：
  - [ ] LSPosed：release 包安装运行，沉浸/过滤/下载无回归（混淆规则变化）
  - [ ] 确认归档 APK 与临时目录产物一致、可安装
- 备注：密钥公开是用户明确决策；`keystore.properties` 中口令明文，进仓库可见

## 基建：debug 包与 release 同签名（2026-09-23）

- commit：`build: debug 包与 release 同签名`
- 修改文件：
  - `app/build.gradle.kts`（修改）：`debug` buildType 增加 `signingConfig`，有 `keystore.properties` 时用 xqe release 密钥，否则回退默认 debug 密钥
- 核心逻辑：用户要求正式与测试同签名，避免两边切换安装时签名冲突；包名仍区分（debug 带 `.debug` 后缀）
- 风险点：无（仅换 debug 包签名身份）
- 真机验证：
  - [ ] debug 包可安装运行（已用 apksigner 确认证书 SHA-256 `46CCA38D…`，与 release 一致）
- 备注：无

## Phase 0：基线分析（2026-09-23，不改代码）

- commit：（无代码提交；构建基建见上两条）
- 基线绿灯：`clean lintRelease test assembleRelease` 通过（69 tasks），产物 `douxianren-lsp-api102-v1.5.1.apk`，xqe 签名
- 阅读范围：16 个源码文件全部（`hook/` 13 个 + `MainActivity`/`FilterPreferences`/`ModuleApplication` + manifest + 构建脚本）

### 当前架构图

```text
DouyinModule (Xposed 入口，installSubsystem 逐个装，单点失败隔离)
├── hookActivityLifecycle / hookActivityTouch / hookInstrumentationLifecycle
│     → ImmersiveUi.onActivityResumed/Paused/before|onActivityTouch
├── DoubleTapGuard（FeedComponentGroup.interceptDoubleClick + GestureDetector 监听）
├── ImmersiveTouchGuard（LongPressLayout 手势 + ViewGroup.dispatchTransformedTouchEvent）
├── hookDouyinActivityLifecycle（SplashActivity onCreate/onResume/onStop）
├── FeedContentTracker（BaseListFragmentPanel 构造 + y0(int) 页面选中）
└── PlayerHooks（TTVideoEngine*/SimplifyPlayerImpl/SimVideoUrlModel）
      → PlaybackState（播放状态机）→ ImmersiveUi.onPlaybackChanged/Completed

ImmersiveUi（主循环 + 编排，1529 行）
├── 100ms scan 循环：error → loopBoundary → filter → hide/restore
├── swipeToNext（合成 MotionEvent 上滑，唯一“下一条”实现）
├── checkCurrentFeedContent → filterCurrentItem（3 次确认后 swipe）
├── 暂停恢复界面 + 下载按钮 → VideoDownloader（MP4/MP3）
└── 触摸解析：点击暂停/恢复确认、上滑切视频确认

MainActivity（纯代码 UI）⇄ RemotePreferences "content_filter" ⇄ 抖音进程
```

### 六条数据流

1. currentAweme：`BaseListFragmentPanel` 构造 hook 建 PANELS 注册表 → `current(decor)` 选最优可见 panel（选中优先 + 面积 + 新近度）→ `getCurrentAweme()` → `snapshot()`；每次调用重新计算，无 Aweme 缓存。
2. playback：引擎方法（play/start/resume、pause/stop/release、*completion*、set*Listener）+ 监听回调（onCompletion、onPlaybackStateChanged 0=停止/1=播放/2=暂停/3=错误/4=初始化）→ `PlaybackState`（playing/userPaused/engine 弱引用/候选池/generation）→ `ImmersiveUi` 回调。
3. filter→next：scan 循环 → `checkCurrentFeedContent`（armed 220ms / fallback 900ms，同一 aid+reason 750ms 窗口 3 次确认）→ `filterCurrentItem` → `swipeToNext`（合成上滑：DOWN + 8 步 MOVE + UP，1500ms 防抖 + 运行锁 + 1200ms 看门狗）。
4. completion→next（**已存在**）：`completed()`/loop 边界（position 回绕检测）/error 确认 → `onPlaybackCompleted` → `swipeToNext`。Phase 2 不是从零做，而是加开关 + reason + 单次语义加固。
5. download：`snapshot.playUrls`（play_addr/h264/bytevc1 的 urlList，不带水印链）→ 选择框 → 单线程池 → MP4 走 MediaStore(Q+)/DownloadManager，MP3 走下载源 + `AudioTranscoder`（MediaCodec 解码 → jump3r 编码 128k/192k）→ 全程 `ensureEnabled` 门控 + Toast。
6. preferences：抖音进程 `getRemotePreferences(NAME)` + 设置 App `XposedService.getRemotePreferences(NAME)`；两份缓存（`FeedContentTracker.filterSettings`、`ImmersiveUi` 四个 flag）+ `OnSharedPreferenceChangeListener` 实时刷新。

### 12 个调查问题（摘要）

1. currentAweme：panel 注册表 + 可见性评分，见上流 1。2. Tracker 生命周期：install 时解析类/方法并 hook 构造与 y0，之后纯查询。3. PlaybackState：播放/用户暂停/引擎/自动切换窗口/进度追踪/错误确认。4. PlayerHooks：5 个引擎类 × 方法名启发式 + listener 对象二次 hook。5. 过滤切下一条：合成上滑（方案 C），见上流 3。6. 下载 URL：snapshot.playUrls，下载器不自己反射。7. FilterPreferences：键 + 默认值 + Values 解析（关键词 split + 小写 contains）。8. MainActivity：XposedService 绑定后读写 RemotePreferences，Switch 即时保存，关键词按钮保存。9. Modern API 入口：`DouyinModule extends XposedModule`，`onPackageReady` 限 `com.ss.android.ugc.aweme` 首包 + ClassLoader 去重。10. DexKit：**零使用**（源码/依赖均无）。11. 主线程：scan/触摸/下载按钮/对话框在 MAIN；播放器回调在各自线程（PlaybackState synchronized）；下载在单线程池。12. 版本变化：按子系统 try/catch 隔离 + 类缺席容忍（ClassNotFound 仅记日志）+ 降级路径（y0 缺席用可见 panel 回退）；版本敏感点：SplashActivity、panel.`y0`/`f`、isLive/awemeType、interceptDoubleClick(40.5)、LongPressLayout 字段。

### 建议新增/修改文件

- 新增 `hook/FeedNavigator.java`（Phase 1，含 Reason 枚举）
- 新增 `hook/feature/AutoNextHook.java`（Phase 2，开关 + 单次语义，复用现有 completion 路径）
- 新增 `hook/model/AwemeAccessor.java`、`hook/model/StatisticsAccessor.java`（Phase 3，把 snapshot() 字段读取搬过去；Snapshot/PlayUrl 保留作传输对象）
- 新增 `hook/config/ConfigProvider.java` + `LsposedConfigProvider.java`（Phase 10 按需；当前两份缓存 + listener 机制先不动）
- `FilterPreferences` 按 Phase 加键（autoNext、exactCount、publishTime/Location、颜色、长视频阈值）；`MainActivity` 按 §二十顺序加开关；新纯逻辑配单测
- 不动：`PlaybackState` 状态机、`PlayerHooks` 引擎表、下载管线、`ImmersiveUi` 隐藏/恢复主体

### Phase 1 最小改造方案（待确认后执行）

- 新建 `FeedNavigator.moveToNext(reason)`，Reason 枚举：`AUTO_PLAY_FINISHED`、`PLAYBACK_ERROR`、`FILTER_AD`、`FILTER_LIVE`、`FILTER_IMAGE`、`FILTER_KEYWORD`、`FILTER_VIDEO`；把 `swipeToNext` 的防抖/运行锁/token/看门狗整体搬入，`ImmersiveUi` 只保留委托调用。
- 接入点：`onPlaybackCompleted` 三处（completion callback→AUTO_PLAY_FINISHED，playback error→PLAYBACK_ERROR，completed loop boundary→AUTO_PLAY_FINISHED）+ `filterCurrentItem`（按 filterReason 前缀映射到 FILTER_*）。
- `PlaybackState.beginAutoSwitch()` 调用保留在 moveToNext 内部，时序不变；合成上滑机制不变（不引入 Pager 内调，Phase 1 只做统一）。
- 验收：过滤/播完/错误三条路径行为与改造前一致；单测全过；`git diff` 仅新增 1 文件 + `ImmersiveUi` 删代码。

## Phase 1：统一 FeedNavigator（2026-09-23）

- commit：`feat: 统一 FeedNavigator，补诊断日志，新增插件内日志查看器`（与诊断日志/查看器合并提交）
- 修改文件：
  - `hook/FeedNavigator.java`（新增）：`Reason` 枚举 7 项 + `moveToNext(decor, reason)`（原 `swipeToNext` 逻辑逐行搬入：防抖/运行锁/token/看门狗/8 步上滑，时序零改动）+ `reasonForFilter` 纯映射 + `isRunning()`；Handler 改为懒加载以便 JVM 单测
  - `hook/ImmersiveUi.java`（修改，净删约 110 行）：删除 `swipeToNext`/`finishSwipe`/`dispatch` 及 3 个状态字段；`onPlaybackCompleted` 签名改为 `Reason`；`filterCurrentItem` 经 `reasonForFilter` 映射；`armContentFilter`/`scheduleScan` 可见性放宽到包内（仅此两处）
  - `hook/PlaybackState.java`（修改 1 行）：completion 回调的 reason 字面量改为 `Reason.AUTO_PLAY_FINISHED`，状态机逻辑不动
  - `hook/FeedNavigatorTest.java`（新增单测）：8 种 filterReason 映射 + 详情后缀容忍 + null/未知回退，3/3 通过
- 核心逻辑：4 个“下一条”入口（播完/错误/loop 回绕/内容过滤）全部收敛到 `FeedNavigator.moveToNext`；reason 映射：completion/loop→AUTO_PLAY_FINISHED，error→PLAYBACK_ERROR，filter 按前缀→FILTER_*；`beginAutoSwitch` 仍在 move 内部、dispatch 前调用，时序与原来一致
- 风险点：低。纯搬移重构；唯一语义变化是日志 reason 从自由串变为枚举名；`dispatch` 的 View 参数契约不变（调用方均已判空）
- 真机验证：
  - [ ] LSPosed：广告/直播/图文/关键词过滤各触发一次，确认正常上滑且只跳一条
  - [ ] LSPosed：普通视频播完确认自动下一条（行为与改造前一致）
  - [ ] 观察 logcat `swipe to next feed item: <REASON>` reason 是否符合预期
- 备注：`git diff` 主体为搬移；新文件 LF、旧文件 CRLF，混排由 git autocrlf 处理

## 基建：诊断日志补齐（2026-09-23）

- commit：`feat: 统一 FeedNavigator，补诊断日志，新增插件内日志查看器`（与 Phase 1/查看器合并提交）
- 审计结论：安装期（子系统隔离 ERROR、tracker/hook 计数 INFO、缺席 WARN）、播放状态迁移、过滤 3 次确认、下载成败、scan 崩溃限流 ERROR 全有；高频反射探测的静默 catch 是故意的（fallback 链，打日志会刷屏）；全 hook 带 setId + PROTECTIVE，框架侧可定位。缺口是导航/过滤的守卫拒绝全静默。
- 修改文件：
  - `hook/FeedNavigator.java`（修改）：`moveToNext` 5 个守卫分别打 `[FeedNav]` debug（module 关闭/进行中/decor 脱离/防抖），坏尺寸打 warn 带宽高
  - `hook/ImmersiveUi.java`（修改 1 处）：`filterCurrentItem` 被拦截时打 `[Filter]` debug
  - `hook/PlaybackState.java`（修改 3 处）：`completed`/`error` 遇到 null/过期 player、`markPlaying` 不收编引擎时打 `[Playback]` debug
- 核心逻辑：只加日志，不改任何行为分支；新日志统一带 `[FeedNav]`/`[Filter]`/`[Playback]` 前缀（任务书 §三十六），旧日志暂不批量改前缀
- 风险点：无。debug 级为主，不影响 release 性能与行为
- 真机验证：
  - [ ] 复现一次过滤，确认出现 `filtering feed item:` + `swipe to next feed item: FILTER_*`
  - [ ] 异常/失效时按下面方法导出日志发给 AI 修复
- 日志导出方法（用户操作）：
  ```powershell
  # 复现问题后立刻执行，保存到文件发给 AI（包含时间戳、模块日志与系统崩溃栈）
  adb logcat -v threadtime -s DouyinImmersive AndroidRuntime:E > douyin-log.txt
  # 只看模块实时日志：
  adb logcat -v threadtime -s DouyinImmersive
  ```
  导出时请附带：抖音版本、复现步骤、当时在播/暂停/过滤/下载哪个场景。

## 基建：插件内日志查看器（2026-09-23，参考 Sesame-M）

- commit：`feat: 统一 FeedNavigator，补诊断日志，新增插件内日志查看器`（与 Phase 1/诊断日志合并提交）
- 修改文件：
  - `hook/LogBook.java`（新增）：logcat + 文件双写；文件落 `Android/media/com.ss.android.ugc.aweme/douyim/log/runtime.YYYY-MM-DD.log`（目标 App 自己的 media 目录，hook 进程免权限可写，与 Sesame-M 同策略）；单守护线程异步写，调用方永不阻塞；失败自动降级为纯 logcat；保留最近 7 天；读侧提供文件列表/尾部读取（级别过滤）/清空
  - 77 个日志调用点迁移：`Log.*`/`module.log`/`log(` 全改为 `LogBook.*`（写侧 API 不变：d/i/w/e × 有/无异常），并删掉 9 个文件的无用 import；`LogBook` 自身的 logcat 调用保留（防递归）
  - `LogViewerActivity.java`（新增）：文件列表 + 级别过滤（全部/信息+/警告+/错误）+ 尾部查看 + 刷新/分享（文本直发，无需 FileProvider/AndroidX）/清空；目录不可读时引导授予存储权限，仍不可读则提示 adb 命令
  - `MainActivity.java`（修改）：高级区加“查看运行日志”入口；`AndroidManifest.xml`：注册 viewer（exported=false）+ `READ_EXTERNAL_STORAGE` 权限
  - `hook/LogBookTest.java`（新增单测）：文件名/保留期/级别解析/排序/尾部过滤/缺失文件，8/8 通过
- 核心逻辑：与 Sesame-M 的差异——①不用 xlog，手写约 300 行零依赖实现（队列 + 按天轮转 + 保留期）；②单文件（Sesame-M 按模块多文件），级别过滤在查看器做；③分享走 `EXTRA_TEXT` 文本直发，不引入 AndroidX FileProvider；④旧日志不批量改前缀，新日志沿用 `[FeedNav]/[Filter]/[Playback]`
- 风险点：Android 9（minSdk 28）上外部目录写需要抖音已获存储权限，否则自动降级纯 logcat（功能不受影响，只是插件内看不到）；Android 13+ 读另一 App 的 media 目录可能被拒，viewer 有降级提示；文件 I/O 全在后台线程，不阻塞 feed 高频路径
- 真机验证：
  - [ ] LSPosed：进抖音播放/过滤后，打开插件“查看运行日志”能看到当天文件与内容
  - [ ] 级别过滤、刷新、分享（文本发出）、清空逐项可用
  - [ ] 拒绝存储权限时有引导；卸载重装/多日后只保留 7 天文件
- 备注：以后导出日志优先用插件内分享；logcat 命令仍有效（双写一致）

## Phase 2：自动下一条开关化（2026-09-23）

- commit：`feat: add auto next after playback completion`
- 修改文件：
  - `FilterPreferences.java`（修改）：新增 `KEY_AUTO_NEXT`/`DEFAULT_AUTO_NEXT(true)`/`readAutoNext`，跟随现有 `readXxx` 风格（不进过滤专用的 `Values`）
  - `MainActivity.java`（修改）：播放卡加“播放完成自动下一条”开关（字段/创建/初值/启停四处接线，与现有开关一致）
  - `hook/ImmersiveUi.java`（修改）：新增 `autoNextEnabled`（`refreshPreferences` 读取，listener 自动跟随）+ `lastAutoNextAid/lastAutoNextFired`；`onPlaybackCompleted` 中 AUTO_PLAY_FINISHED 受开关门控 + aid 单次守卫，PLAYBACK_ERROR 保持原行为（错误恢复不受开关影响）；新增 `currentAid` 辅助（异常 fail-open）
  - `hook/FeedNavigator.java`（修改）：新增纯函数 `shouldFireAutoNext(aid, lastAid, alreadyFired)`（aid 为空放行、aid 变化放行、同 aid 只放行一次）
  - `hook/FeedNavigatorTest.java`（修改）：加 3 个单测（单次/切换重放/无 aid 放行），6/6 通过
- 核心逻辑：开关默认开 = 与改造前行为完全一致（播完/回绕自动下一条）；关闭后停在末尾。单次语义按 aid 判定：同一视频重复 completion 只触发一次；切走再回来 aid 变化可重新触发；取不到 aid 时放行（与原来一致）
- 风险点：低。`currentAid` 每次 completion 调一次 `snapshot()`（反射，非高频路径，可接受）；开关关闭是唯一行为变化点
- 真机验证：
  - [ ] LSPosed：开关开时播完自动下一条（与改造前一致），且同一视频不连跳两条
  - [ ] LSPosed：开关关时播完停在末尾；过滤（广告/关键词）仍正常跳过（不受此开关影响）
  - [ ] 观察 `[AutoNext]` 日志：disabled / already fired / aid 变化三分支
- 备注：发现总开关（module_enabled）目前只门控沉浸/暂停恢复，不管过滤与自动下一条——与 AGENTS.md“总开关关闭时全部停用”不一致，属历史行为， Phase 2 未动，等用户定是否改（后续核实见“决策”条）

## 决策：范围与总开关（2026-09-23）

- commit：`docs: 范围与总开关决策`
- 决策 1（范围）：图集下载、分享链接解析下载、评论过滤、弹幕屏蔽、直播录制、倍速/记忆进度/定时关闭等一律不做；长视频过滤按计划做（属 13 项内）。计划文档与 AGENTS.md 无需修改。
- 决策 2（总开关）：经 grep 核实，`module_enabled` 门控触摸入口（185/195/217）、播放回调（421/461）、scan 主循环（513）、下载按钮（642/678）、手势与隐藏（1263+），且过滤/自动下一条经 `moveToNext` 卡点也被门控——即总开关关闭时检测停、动作停、界面恢复，AGENTS.md“总开关关闭时全部停用”成立。Phase 2 记录中的“不一致”判断是误判，以本条为准，不做任何改动。

## Phase 3：AwemeAccessor 数据抽象（2026-09-23）

- commit：`refactor: add aweme data accessors`
- 修改文件：
  - `hook/model/AwemeAccessor.java`（新增）：反射工具（readField/invokeNoArg/SerializedName 缓存读）+ 类型 coercion（int/boolean/collection/string/text/firstNonBlank）+ URL 解析（resolvePlayUrls 三源去重）全部从 tracker 逐行搬入；新增 13 个具名 getter（aid/awemeType/isAd/rawAd/isLive/isImage/isMultiImage/isSlides/video/article/images/imageInfos/title/description），其中 `isLive()` 内含 `awemeType == 101` 回退
  - `hook/FeedContentTracker.java`（修改，净删约 150 行）：`snapshot()` 改为调 accessor getter + 保留过滤策略（photo 合成/reason 映射/DTO 组装）；删老 helper 与 `SERIALIZED_FIELDS`；`FeedContentTracker`/`PlayUrl` 放宽为 public（跨包引用），`Snapshot` 保持包内
- 核心逻辑：纯搬移，零行为变化；读（accessor）与策略（tracker）分离，后续 Phase 只在 accessor 加 getter
- 未做：`StatisticsAccessor` 顺延到 Phase 4（当前无 statistics 读取，新建空类违反“不搭空架子”）
- DYHelper APK 结论（docs/my/DYHelper1.6.8.apk，jadx 粗看）：包 `com.example.dyhelper`，LSPosed API 101/102，重度混淆 Kotlin+Compose；scope 覆盖抖音/抖音极速/火山（aweme.mobile）/小红书；功能面=社交直播助手（福袋统计参与、私信时间标签、特别关心/隐藏联系人、营地种地统计、评论关键字过滤、表情管理、作品快捷操作、手势倍速、小红书原图）。与本模块纯净播放方向交集小，且混淆严重，**不深入逆向其 Hook 实现，只作功能面参考**；用户要的 6 项里评论过滤/倍速 DYHelper 有但用户没点要，按“复杂算了”不做
- 单测：不新增文件——现有 17 个 `FeedContentTrackerTest` 经 FakeAweme 全穿新代码路径，17/17 通过即覆盖
- 风险点：极低。反射顺序/回退链逐行一致；`isLive` 的 101 规则从 tracker 移入 accessor，语义不变
- 真机验证：
  - [ ] LSPosed：过滤/下载/详情正常（行为应与改造前一致，logcat 无新增异常）

## 范围增补：用户 6 项确认（2026-09-23）

- commit：`docs: 范围增补与 DYHelper 结论`
- 用户要的 6 项对照：①自动下一条=Phase 2✓；②过滤=已有+Phase 8✓；③隐藏发布按钮/顶部 TAB=**新增 Phase 11**；④精确人数=Phase 4✓；⑤时间地点常显+颜色=Phase 5/6/7✓；⑥无水印下载=已有✓，**链接复制并入 Phase 9**。计划 13 的“UI 常显”由 Phase 4-6 常显 + 暂停恢复界面覆盖，不再单做。
- 不做：图集/LivePhoto/分享链接解析/评论过滤/弹幕/直播录制/倍速/批量下载（用户确认：复杂算了）。
- Phase 11（界面元素隐藏）：发布按钮独立开关 + 顶部 TAB 按文本关键词隐藏（默认空，用户按日志报 TAB 文后配）；ordering 放在 Phase 9 之后、Phase 10 之前执行。

## Phase 4：精确人数（2026-09-23）

- commit：`feat: show exact engagement counts`
- 修改文件：
  - `hook/model/StatisticsAccessor.java`（新增）：statistics 对象具名读（方法→字段→SerializedName 三级回退），缺失回 -1 区分“未知”与“0”
  - `hook/model/AwemeAccessor.java`（修改）：新增 `statistics()` + `longValue`/`doubleValue`
  - `hook/FeedContentTracker.java`（修改）：Snapshot 加 5 个 long（digg/comment/collect/share/play），`snapshot()` 解析（fail-open 全 -1）
  - `hook/ExactCounts.java`（新增）：每 scan 找右侧数字 TextView，按“缩写一致（5% 容差）+纵序”映射到点赞/评论/收藏并改写精确值；原值 WeakHashMap 保存，aid 切换/关开关/非普通视频时恢复；每次改写打 INFO（含 aid/前后值）
  - `hook/ImmersiveUi.java`（修改）：`exactCountsEnabled` 开关 + scan 钩子 + aid 变化时打 `[Statistics]` INFO（先日志验证）
  - `FilterPreferences`/`MainActivity`：`KEY_EXACT_COUNTS`（默认开）+ 开关接线
  - 单测：`StatisticsAccessorTest` 5/5（方法优先/SerializedName/缺失/int 强转），`ExactCountsTest` 4/4（解析/容差/拒绝），`FeedContentTrackerTest` 17/17 穿新 DTO
- 核心逻辑：分享数无 UI 位只记日志；改写三重保险（普通视频限定+右侧区域+缩写一致），对不上就不写只记 debug
- 风险点：中。右侧三项纵序=点赞/评论/收藏是基于现版本布局的假设；若版本调序，缩写一致检查只能防数量级错误，防不住同量级串位——靠 `[Counts]` 日志人工核对
- 真机验证：
  - [ ] LSPosed：`[Statistics]` 行 5 个数非 -1 且与视频量级相符（先验证数据层）
  - [ ] LSPosed：暂停界面点赞/评论/收藏显示完整数字；切视频后恢复+重写正确；广告/直播/图文不改写
  - [ ] 开关关后数字恢复缩写

## Phase 5：发布时间常显（2026-09-23）

- commit：`feat: keep publish time visible`
- 修改文件：
  - `hook/model/AwemeAccessor.java`（修改）：新增 `createTimeMs()`（getCreateTime→createTime→create_time，秒/毫秒自适应归一化，无效回 -1）
  - `hook/FeedContentTracker.java`（修改）：Snapshot 加 `createTimeMs`
  - `hook/PublishInfo.java`（新增）：自有 overlay TextView（左上，不拦触摸），播/停两态常显“发布于 yyyy-MM-dd HH:mm”（设备时区）；广告/无时间/关开关时移除；aid 变化打 debug
  - `hook/ImmersiveUi.java`（修改）：`publishTimeEnabled` 开关 + scan 钩子
  - `FilterPreferences`/`MainActivity`：`KEY_PUBLISH_TIME`（默认开）+ 开关接线
  - 单测：`PublishInfoTest` 4/4（格式/时区/坏输入/空快照），`FeedContentTrackerTest` +2（秒转毫秒/缺失），共 19/19
- 核心逻辑：不用找抖音自带时间控件（feed 界面不可靠），自建 overlay 最稳；Phase 6 往同一 overlay 追加地点，Phase 7 配色
- 风险点：低。overlay 位置（左上 12/96dp）未在真机验证过，可能与顶部 TAB/标题重叠——待用户截图反馈后调
- 真机验证：
  - [ ] LSPosed：播/停两态左上都显示发布时间且时间正确；切视频更新；广告不显示
  - [ ] 开关关后 overlay 消失；沉浸播放时不挡画面中央

## Phase 6：IP 属地/POI/地点显示（2026-09-23）

- commit：`feat: show available publish location metadata`
- 修改文件：
  - `hook/model/AwemeAccessor.java`（修改）：新增 `author()`/`poi()` 嵌套对象读 + `ipLabel()`（Aweme 直读 ipLabel/ipLocation/ipAttribution 三组→回退 author 的 ipLabel/ipLocation）/`poiName()`（直读→poi 对象的 poiName/name）/`location()`（location/address）/`city()`（直读 city/cityName→回退 poi）；统一经私有 `textOn()` 做 方法→字段→SerializedName 三级回退，缺失回空串
  - `hook/FeedContentTracker.java`（修改）：Snapshot 加 `ipLabel/poiName/location/city` 4 个 String（未知即空串）
  - `hook/PublishInfo.java`（修改）：`update/composeText` 加 `timeEnabled/locationEnabled` 双开关；overlay 改多行（maxLines 3）；地点行优先级 POI>location>city（`displayPlace()`），IP 属地独立一行标“IP属地：”绝不叫地点；无任何已知项时移除
  - `hook/ImmersiveUi.java`（修改）：`publishLocationEnabled` 开关 + scan 钩子（任一开即 update，全关才 remove）+ aid 变化时打 `[PublishInfo]` INFO（ipLabel/poi/location/city 四值，先日志验证字段）
  - `FilterPreferences`/`MainActivity`：`KEY_PUBLISH_LOCATION`（默认开）+ 开关接线
  - 单测：`FeedContentTrackerTest` +5（直读/city/getter 优先/author 回退/嵌套 poi/全缺失），共 24/24；`PublishInfoTest` 9/9（三行拼接/空隐藏/双开关/ad 隐藏/地点优先级）；全量 73/73
- 核心逻辑：字段名未经真机验证（候选名按抖音模型常见命名覆盖），数据层 fail-open、UI 只渲染非空，杜绝伪造；`[PublishInfo]` 日志是字段验证依据——用户导出后可凭此收敛候选名
- 风险点：中。候选字段名可能全 miss（overlay 只显示时间行，属预期降级）；author 嵌套读在部分版本可能为空；overlay 三行时遮挡面变大，位置待真机反馈再调
- 真机验证：
  - [ ] LSPosed：先看 `[PublishInfo]` 行四值是否非空（定字段真实性），再看 overlay 行数与标注（IP 属地/地点语义区分）
  - [ ] 两开关独立：只关地点则剩时间行；全关 overlay 消失；广告不显示
  - [ ] 切视频后地点更新（不串显上一条）

## Phase 7：文本颜色可配（2026-09-23）

- commit：`feat: add configurable feed text colors`
- 修改文件：
  - `hook/FeedUiStyle.java`（新增，public 纯逻辑）：`parseColor`（#RGB/#RRGGBB/#AARRGGBB，可省 #，非法回 fallback）/`isValidColor`/`toHex`（#RRGGBB 大写）
  - `hook/PublishInfo.java`（修改）：新增 `OverlayContent`（text + 时间/地点 span 区间，地点段覆盖 IP 属地+地点两行）；`composeText` 改调 `composeOverlay`（旧单测零改）；`update` 加 `customColors/timeColor/locationColor`，开时按区间套 ForegroundColorSpan，颜色变化也触发重设（`lastStyleKey` 语义）
  - `hook/ExactCounts.java`（修改）：`apply` 加 `customColors/countColor`；改写数字时缓存原 `ColorStateList` 并覆盖颜色（只在变化时 set，避免每 scan 重绘），`restoreAll`/关开关时恢复原色
  - `hook/ImmersiveUi.java`（修改）：`customColorsEnabled` + 三色 int（默认全白 `0xFFFFFFFF`）+ 透传
  - `FilterPreferences`（修改）：`KEY_CUSTOM_TEXT_COLORS`（默认关）+ 三色 key（int 存储，默认全白）+ reader
  - `MainActivity.java`（修改）：播放卡加总开关 + 新增“文本颜色”卡（3 个 hex 输入 + 保存按钮，非法 toast 拒存）
  - 单测：`FeedUiStyleTest` 6/6（3/6/8 位/非法/roundtrip），`PublishInfoTest` +4（区间三线/无时间/无地点/空），全量 83/83
- 核心逻辑：颜色只覆盖“我们改写的两处”（overlay 自有 view 直接 span，首次创建即白字；精确数字缓存原 ColorStateList 以便恢复）；总开关关=零覆盖，与改前行为一致；View glue（setTextColor/span）保持薄，不可 JVM 单测部分靠真机验证
- 风险点：低。数字原色按 view 缓存，RecyclerView 复用串色时下次 bind 重算（apply 每 scan 跑）；overlay 默认白与原来一致
- 真机验证：
  - [ ] LSPosed：总开关开+改三色，数字/时间/地点分别变色；总开关关后数字恢复抖音原色、overlay 恢复白字
  - [ ] 切视频/复用后无串色；非法 hex 保存被拒并 toast

## Phase 8：长视频过滤（2026-09-23）

- commit：`feat: add long video filter`
- 修改文件：
  - `hook/model/AwemeAccessor.java`（修改）：新增 `getDurationMs()`（video 对象 duration/durationMs 三级回退→Aweme 直读兜底；(0,1000) 视为秒×1000，其余按毫秒，无效回 -1）+ 私有 `longOn()`（与 `textOn()` 对称）
  - `FilterPreferences.java`（修改）：`KEY_SKIP_LONG_VIDEOS`（默认关）+ `KEY_LONG_VIDEO_THRESHOLD_MS`（默认 180_000，即 3 分钟）；`Values` 加 `skipLongVideos/longVideoThresholdMs` 两字段（6 处单测构造同步更新）
  - `hook/FeedContentTracker.java`（修改）：Snapshot 加 `durationMs`；普通视频分支先判长视频（开+已知+超阈值→`long video model`），未知时长放行；`classificationDetails()` 追加 durationMs 供日志核对
  - `hook/FeedNavigator.java`（修改）：`Reason` 加 `FILTER_LONG_VIDEO`，`reasonForFilter` 加 `long video` 前缀映射（与既有 `long article model`→FILTER_IMAGE 无冲突）
  - `MainActivity.java`（修改）：跳过类型卡加“长视频”开关 + “长视频阈值（秒）”输入与保存按钮（5~7200 秒，非法拒存）；`addColorField` 更名 `addLabeledField` 复用
  - 单测：`FeedContentTrackerTest` +5（超阈值过滤/阈值下放行/未知放行/开关关放行/秒转毫秒），共 29/29；全量 88/88
- 核心逻辑：只移植“时长阈值”判断思路（FreedomPlus 参考项），过滤动作走统一 FeedNavigator；阈值存毫秒、UI 按秒输入输出
- 风险点：低。duration 字段名未经真机验证，miss 时恒为未知=放行（配合 `[Filter]`/classification 日志可核对）；秒/毫秒启发式在 1 秒分界，极端短视频（<1s）会被×1000，但不影响“超阈值”判定方向
- 真机验证：
  - [ ] LSPosed：开+阈值 180s，刷到 3 分钟以上视频被跳过且 reason 为 long video；短视频/未知时长正常播放
  - [ ] 开关关后长视频不再跳过；阈值改小（如 10s）后短视频也被跳过（验证阈值生效）

## Phase 9：复制当前视频链接（2026-09-23）

- commit：`feat: copy current video link`
- 差异分析（任务书 §十三要求）：抖仙人已有 MP4 无水印直链下载 + MP3 转码 + 下载队列/进度提示，覆盖用户要的“无水印下载”；图集/LivePhoto/分享链接解析按“范围增补”决策不做。故本 Phase 只补缺失能力——**复制链接**，不动下载管线。
- 修改文件：
  - `hook/VideoDownloader.java`（修改）：新增 `copyLink()`（剪贴板写入 + toast，日志只记 aid 与长度不记 URL 明文）+ 纯函数 `resolveCopyLink()`（首个 playUrl）；附带把静态 `MAIN` Handler 改懒加载（沿用 FeedNavigator 先例，使 JVM 单测可加载该类，原下载行为不变）
  - `hook/ImmersiveUi.java`（修改）：暂停态“复制链接”椭圆按钮（样式/生命周期与下载按钮镜像：8 处 show/remove 点全部对齐），位置优先叠在下载按钮下方、空间不足改叠上方；`copyLinkEnabled` 开关门控
  - `FilterPreferences`/`MainActivity`：`KEY_COPY_LINK`（默认开）+ 开关接线
  - 单测：`VideoDownloaderTest` 2/2（取首链/空快照空列表回 null）；全量 90/90
- 核心逻辑：复制的是与下载同一来源的无水印直链（`snapshot.playUrls[0]`），无地址时 toast 而不是复制空串
- 风险点：低。按钮位置依赖 `resolveDownloadButtonTop` 同一结果，两按钮同行右侧竖排；极小屏幕极端位置可能重叠（已按上下空间二选一）
- 真机验证：
  - [ ] LSPosed：暂停后出现复制按钮且不与下载按钮重叠；点击 toast“链接已复制”，粘贴得无水印直链
  - [ ] 无地址视频点复制 toast“暂不可用”；开关关后按钮消失；恢复播放后按钮消失

## Phase 11：界面元素隐藏（2026-09-23）

- commit：`feat: hide publish button and top tabs`
- 修改文件：
  - `hook/FeedUiHider.java`（新增）：发布按钮按几何启发式找（底部 20% + 横向居中 ±20% + 宽≤18%/高≤12% + clickable 或 contentDesc 含“发布”）后 GONE；顶部 TAB 按关键词对顶部 22% 短文本（≤12 字）contains 匹配后 GONE；双 WeakHashMap 独立跟踪原 visibility（不用沉浸式 alpha 注册表），关开关/关键词清空/总开关关时恢复；每次隐藏打 `[FeedUi]` INFO（view 类名），TAB 失配自动恢复
  - `hook/ImmersiveUi.java`（修改）：`hidePublishEnabled` + `hideTabKeywords`（refresh 解析）+ scan 钩子（PublishInfo 块之后、沉浸分支之前，两分支都跑）；总开关关两处 restoreAll
  - `FilterPreferences`（修改）：`KEY_HIDE_PUBLISH`（默认关——盲找保守）+ `KEY_HIDE_TABS`（默认空=不隐藏）+ reader
  - `MainActivity.java`（修改）：新增“界面元素”卡（发布按钮开关 + TAB 关键词多行输入 + 保存按钮）
  - 单测：`FeedUiHiderTest` 7/7（发布几何/描述信号/关键词匹配/大小写/长文本拒绝/解析去重）；全量 97/97
- 核心逻辑：fail-open——找不到就不藏；GONE 而非 alpha=0（TAB 行可回流）；关时全恢复
- 风险点：中高（盲写）。发布按钮几何阈值未经真机验证，可能藏错（靠 `[FeedUi]` 日志 + 关开关恢复兜底）；TAB 只藏 TextView 本体，可点击容器可能残留（v1 局限，待反馈）；两特性全开时每 scan 多两次遍历（默认全关零开销）
- 真机验证：
  - [ ] LSPosed：开隐藏发布按钮，底部“+”消失且无其他元素被藏（看 `[FeedUi]` 日志确认 view 类名）；关后恢复
  - [ ] TAB 关键词填“商城”，顶部对应 TAB 文字消失；改错词/清空后恢复；长描述含关键词不被藏
  - [ ] 总开关关后两处全部恢复

## Phase 10：LSPatch 兼容交付（2026-09-23）

- commit：`fix: survive unavailable framework services (lspatch prep)`
- 结论先行：无真机可测，按任务书“只有真实失败点才写兼容代码”——**不引入 ConfigProvider/LocalConfigProvider**，只做两处 LSPosed 零行为变化的加固 + 测试清单。ConfigProvider 留待用户实测触发方案 B 时再做。
- 修改文件：
  - `hook/DouyinModule.java`（修改）：`getRemotePreferences` 包 try/catch，失败记 `[LSPatch]` warn 跑默认值（此前抛异常会导致整个 tracker 子系统安装失败）
  - `ModuleApplication.java`（修改）：`registerListener` 包 try/catch，框架缺席时记 warn 不崩（设置页本就支持无 service 禁用态）
  - `docs/my/LSPatch测试清单.md`（新增）：审计结论 + 5 项加载门禁 + 8 项功能矩阵 + 回报格式 + 方案 A/B/C 触发条件
  - 单测：无新增（框架 API 不可 JVM 覆盖）；全量 97/97
- 风险点：无（LSPosed 路径行为零变化）。最大未知数是 LSPatch 对 API 102 Modern API 的支持度，清单第 1 节覆盖。
- 真机验证：
  - [ ] LSPatch Local：按清单第 1 节逐项打勾，任一失败按第 4 节回报
  - [ ] LSPosed 回归：两处加固不改变现有行为（开关/过滤/下载抽查一次）

## 跟进：DYHelper 深挖结论（2026-09-23，不改代码只记录方法）

- 方法：解包 `docs/my/DYHelper1.6.8.apk`（`com.example.dyhelper` v1.6.8，未开源）取 `classes.dex` 字符串表（ASCII 39k + 中文 8.9k），只提取标识符/功能面字符串做对照，**未复制任何代码**（许可证不明，遵守“只参考思路”）。
- 证实我方已有链正确：`createTime/create_time/getCreateTime`、`digg_count/comment_count/share_count/collect_count`、`play_addr(_h264)/urlList/getUrlList`、`getAwemeRawAd`、`awemeType/isLive/isImage/isAd/statistics/author/desc/video/images`、`getDuration/duration`、`TTVideoEngine/onCompletion`、`FeedRightScaleView`、`LongPressLayout` 全部在对方串表中出现。
- 发现我方缺失（已在下条提交补齐）：`ip_location_string`、`getPoiStruct/poiStruct/poi_struct`、`poiAddress/poiCity`、`awemeCity/awemeRegion`。
- 功能面对照：对方“自动下滑/精确数量/评论关键词过滤/长按倍速/下载/TAB 过滤/描述下方时间地点/IP 属地”中，属用户 6 项的我方已全覆盖；评论过滤/倍速/右侧栏屏蔽按既有决策不做；对方用 DexKit 定位发布按钮（我方无 DexKit 依赖，沿用几何启发式+日志）；对方发布时间放在描述下方（体验更优，但盲找描述容器风险高，待真机截图后再定是否迁移 overlay 锚点）。
- 备注：`videoDuration/item_duration/authorIpLocation` 疑似对方自有命名，未采纳；对方连 duration 都用结构绑定而非写死名，佐证我方“候选链 + fail-open”路线。

## 跟进①：IP/POI/城市字段对齐（2026-09-23）

- commit：`fix: cover observed ip/poi/city field names`
- 修改文件：
  - `hook/model/AwemeAccessor.java`（修改）：`poi()` 改 `getPoiStruct` 优先 + `poiStruct/poi_struct`；`ipLabel()` 加 `ip_location_string`（Aweme 与 author 两级）；`location()` 加 poi 嵌套 address/poiAddress；`city()` 加 `awemeCity` 直读 + poi 嵌套 poiCity + `awemeRegion/region` 兜底
  - 单测：`FeedContentTrackerTest` +5（ip_location_string/getPoiStruct/awemeCity/嵌套 poiCity/嵌套 poiAddress）；全量 102/102
- 核心逻辑：纯加候选分支，不删旧分支，排序按“DYHelper 串表证据强度”（snake 实名 > 推测名）；miss 代价仅一次缓存反射
- 风险点：极低。`[PublishInfo]` 日志仍是最终验证依据
- 真机验证：
  - [ ] LSPosed：之前 `[PublishInfo]` 为空的项重看是否出值；overlay 地点行是否新增显示

## 跟进②：overlay 时间行附带时长（2026-09-23）

- commit：`feat: append video duration to publish time line`
- 修改文件：
  - `hook/PublishInfo.java`（修改）：时间行已知时长时追加 `· m:ss`（`formatDuration`，小时以上 `h:mm:ss`，仍在时间 span 区间内，不占新行）
  - 单测：`PublishInfoTest` +2（formatDuration 四例/后缀拼接），helper 加 durationMs 参数；全量 104/104
- 核心逻辑：复用 Phase 8 已有 `durationMs`，未知时不显示；方便对照长视频阈值
- 风险点：无。时间行变长，overlay 自适应宽度
- 真机验证：
  - [ ] LSPosed：时间行尾部出现 `· m:ss` 且与视频时长相符；未知时长时无后缀

## 修复：overlay 短暂隐藏后不再重现（2026-09-23）

- commit：`fix: reattach publish overlay after transient hide`
- 现象（用户真机反馈）：切视频后 overlay（时间/地点）不再显示。
- 根因：`PublishInfo.update` 在内容短暂为空（切视频间隙快照为 null / 广告 / 无时间无地点）时 remove 了 overlay，却没清 `overlay/overlayDecor` 缓存；下次有内容时复用已脱离父容器的旧 view，setText 生效但永远不可见。
- 修改文件：
  - `hook/PublishInfo.java`（修改 3 行）：空内容分支加 `overlay.clear()/overlayDecor.clear()`；复用条件加 `view.getParent() == null` 即重建（双保险）
  - 单测：无新增（View 挂载属 Android glue，不可 JVM 覆盖）；全量 104/104
- 风险点：极低。只影响 overlay 重建路径
- 真机验证：
  - [ ] LSPosed：连续切 10 条视频（含广告），overlay 每条都正常出现/更新
  - [ ] 若时间行在、仅地点行消失——属该视频无地点数据（按设计隐藏），请导出 `[PublishInfo]` 日志确认

## 跟进③：设置页离线可改，连接后同步（2026-09-23）

- commit：`feat: stage settings edits offline and sync on connect`
- 起因（用户要求）：去掉“未连接不能修改”限制。
- 修改文件：
  - `MainActivity.java`（修改）：启动即绑定本机 SharedPreferences（同名 `content_filter`，MODE_PRIVATE）并启用全部控件；未连接时保存进本机并记 dirty key，连接后自动 push 到远端并 toast 件数；连接中每次保存双写本机做新鲜缓存；`getRemotePreferences` 加 try/catch；原 bind 后半段抽 `refreshControls()`；保存 toast 离线时追加“（连接后同步）”
- 核心逻辑：生效仍依赖连接（binder 是唯一跨进程写通道，无替代方案），但编辑不再阻塞；push 触发远端 listener，hook 侧实时生效，无需重启抖音
- 风险点：低。hook 侧零改动；在线行为与之前一致（多一次本机镜像写）
- 真机验证：
  - [ ] 未装/未启用 LSPosed 时打开设置页：可改，toast 带“连接后同步”，状态条橙字
  - [ ] 启用模块后重进设置页：toast“离线修改已同步”，抖音侧行为跟随新值，无需重装

## 跟进④：overlay 单行化 + 描述下方锚定（2026-09-23）

- commit：`feat: single-line overlay anchored below description`
- 起因（用户要求）：字小一点、去背景、一行、只要时间地点不要前缀、放描述下面。
- 修改文件：
  - `hook/PublishInfo.java`（修改）：拼装改空格单行（`时间[ · 时长] IP 地点`，无前缀标签）；字号 12→10sp，去背景加深阴影，单行 + 尾部省略；新增描述锚点（遍历 TextView 按快照 desc/title 文本匹配，最长优先、aid 切换重搜，命中打 `[PublishInfo] anchor` INFO），锚定失败回退左上；位置变化才 setLayoutParams
  - `MainActivity.java`（修改）：两开关描述同步新位置
  - 单测：`PublishInfoTest` 重写格式断言 +4（截断/后缀/标题回退/短文本拒绝/空白不敏感）；全量 108/108
- 核心逻辑：颜色 span 区间逻辑不变（时间段 + 地点段）；沉浸隐藏下描述 view 仍在布局中，坐标有效
- 风险点：中。描述匹配启发式未经真机验证（短描述 <8 字恒回退；评论区展开时可能误命中评论）；靠 anchor 日志核对，错位不影响功能只影响位置
- 真机验证：
  - [ ] LSPosed：overlay 在描述正下方单行小字无背景；切视频跟随移动；评论区打开时位置仍合理
  - [ ] 看 `[PublishInfo] anchor` 行确认命中的 view 类名；短描述视频回退左上属预期

## 跟进⑤：播放进度条常显（2026-09-23）

- commit：`feat: keep playback progress bar visible`
- 起因（用户要求）：下面的播放进度条一直显示。
- 修改文件：
  - `hook/ProgressBarClassifier.java`（新增，纯逻辑）：类名含 progress/seekbar + 底部 15% + 宽>50% + 高<3% 四重判定
  - `hook/ImmersiveUi.java`（修改）：`showProgressBar` 开关 + scan 保留链（`collectVisibleProgressViews`，类名前置过滤 + 命中去重 INFO `[FeedUi] preserve progress view=`）
  - `FilterPreferences`/`MainActivity`：`KEY_SHOW_PROGRESS`（默认开）+ 开关接线
  - 单测：`ProgressBarClassifierTest` 3/3；全量 111/111
- 核心逻辑：只做“免于沉浸隐藏”（加入 preservedViews），不强行 VISIBLE——暂停态本就 restoreAll 全显；若抖音原生会自动隐藏进度条，待用户反馈再决定是否加强制显示
- 风险点：低。误命中只会多保留一个 view（不隐藏），不破坏功能；方形 spinner 被高/宽比排除
- 真机验证：
  - [ ] LSPosed：沉浸播放时底部进度条可见且随播放推进；看日志确认命中的 view 类名
  - [ ] 开关关后进度条随其他 UI 一起隐藏

## 跟进⑥：属地链按实测证据重排 + 诊断字段（2026-09-23）

- commit：`fix: reorder ip chain by observed evidence`
- 起因（用户反馈）：属地有的视频读不到。深挖 DYHelper dexdump：其 JSON 侧读取顺序为 `ip_location → ipLocation → ip_location_string → location`，另有 `ip_attribution` 路径与对象侧 `{"ipLocation","ip_location"}` 读取；另见 `authorIpLocation` 串。
- 修改文件：
  - `hook/model/AwemeAccessor.java`（修改）：`ipLabel()` 按证据重排（ipLocation 组 → ip_location_string → ipAttribution 组 → authorIpLocation → ipLabel 兜底），author 侧对称加 ipAttribution；新增 `simpleName()`
  - `hook/FeedContentTracker.java`（修改）：Snapshot 加 `awemeClass/authorClass/poiFound` 诊断字段
  - `hook/ImmersiveUi.java`（修改）：`[PublishInfo]` 行追加三诊断值
  - 单测：+3（ip_location 优先/authorIpLocation/诊断字段）；`PublishInfoTest`/`VideoDownloaderTest` 构造同步；全量 114/114
- 核心逻辑：键名已与对方取并集；`ipLabel` 对方串表无命中，降级为最后兜底；未引入 JSON 源（模型字段与 JSON 同源，对象侧读到即够；若诊断显示对象缺失再议）
- 风险点：极低。纯加分支 + 重排 + 日志
- 真机验证：
  - [ ] LSPosed：之前无属地的视频重看；仍无的把 `[PublishInfo]` 整行发回（含 awmCls/authorCls/poiFound 即可定位是对象缺失还是字段缺失）

## 修复：overlay 上下乱跳（2026-09-23）

- commit：`fix: keep overlay in lower screen area`
- 现象（用户真机反馈）：有时候时间地点在顶部，有时候在底部。
- 根因：描述锚点匹配不上（短描述/无描述）时回退到了左上角（12/96dp），与锚定到底部的成功 case 来回跳。
- 修改文件：
  - `hook/PublishInfo.java`（修改）：回退位改左下固定（left 12dp，底部上 180dp）；锚点候选加下半屏过滤（top < 50% 高视为 TAB/评论区误命中，直接排除）；日志文案同步
  - 单测：无新增（位置属 Android glue）；全量 114/114
- 风险点：低。回退位盲定 180dp，可能与短描述/底部按钮轻微重叠，待截图微调
- 真机验证：
  - [ ] LSPosed：连续刷 20 条，overlay 不再出现在顶部；短描述视频落左下固定位且不挡关键按钮

## 跟进⑦：直播 tab 内不跳过直播（2026-09-23）

- commit：`feat: exempt live tab from live filtering`
- 起因（用户要求）：过滤直播只在推荐 tab 生效，进直播 tab 就是要看直播，不应跳过。
- 修改文件：
  - `hook/TabDetector.java`（新增）：顶部 25% 内文本精确等于“直播”且自身/两级祖先 selected 即判直播 tab
  - `hook/ImmersiveUi.java`（修改）：`checkCurrentFeedContent` 里 live + 直播 tab 则 reason 置空走正常接纳（`[Filter] keep live` 按 aid 限流记一条）
  - `MainActivity.java`（修改）：直播开关描述同步
  - 单测：`TabDetectorTest` 3/3；全量 117/117
- 核心逻辑：只豁免 FILTER_LIVE 一路（广告/图文/关键词/长视频在直播 tab 照旧）；检测零命中=维持原行为（fail-close，保证推荐页过滤不受影响）；检测只在出现直播内容时才跑，平时零开销
- 风险点：低。若某版本 tab 文案/selected 语义变化，最坏退化为“直播 tab 也跳过”（即现状）
- 真机验证：
  - [ ] LSPosed：推荐页刷到直播仍跳过；切直播 tab 后直播正常播放且日志有 keep live 行
  - [ ] 关注等其他 tab：直播仍跳过（只豁免直播 tab）

## 回退：overlay 固定回左上角（2026-09-23）

- commit：`fix: pin overlay back to top-left`
- 起因（用户真机反馈）：时间和地点有时显示到文本上（描述锚定压住了 feed 文字）。
- 修改文件：
  - `hook/PublishInfo.java`（修改）：删除整套描述锚点（搜索/定位/日志/字段，约 150 行），回固定左上 12/96dp；单行小字无背景样式保留
  - `MainActivity.java`（修改）：两开关描述改回左上角
  - 单测：删除 4 个 `isDescriptionMatch` 用例（代码已删）；全量 113/113
- 核心逻辑：位置经两轮真机证明“描述下方”不可靠（先乱跳、再压字），退回最稳的固定角；如需再调位置，必须先有截图定坐标
- 风险点：无（回到验证过的老位置）
- 真机验证：
  - [ ] LSPosed：overlay恒在左上，不压任何文字；切视频稳定跟随更新

## 跟进⑧：地点编码离线解析 + 在线兜底（2026-09-23）

- commit：`feat: resolve place adcode offline with online fallback`
- 起因（用户反馈）：地址编码识别还是有问题。开源调研结论：FreedomPlus `AwemeExt.cityInfo()` 证实 `Aweme.city` 是 6 位行政区划编码（非地名），它走 `http://api.ip33.com/Area_Code/GetArea/?code=` 在线解析省/市/区；其 `ipAttribution`/`ipLocation` 仅存根声明、代码中未实际使用。结论：之前 overlay 直接显示了原始编码
- 修改文件：
  - `hook/AdcodeTable.java`（新增，生成文件3351条）：modood 区划 CSV 展平为 `编码=省 市 区`，二分查找；港澳台仅顶级编码
  - `hook/AdcodeResolver.java`（新增，纯逻辑）：`normalize`（2/4/6位→6位，全0/非数字拒绝）→ 离线精确→市级→省级回退；在线 ip33 兜底（单线程/内存缓存256/失败10分钟冷却，手写 JSON 提取省/市/区，不依赖 org.json 以便 JVM 单测）
  - `hook/PublishInfo.java`（修改）：`displayPlace` 经 `resolvePlacePart` 解析（真名直通；未知编码显示原文 fail-open 便于上报）；`update` 新增 `onlineLocationEnabled` 参数并触发 `prefetchOnline`（scan 100ms 下次自动刷新，无需显式刷新接线）
  - `FilterPreferences`/`ImmersiveUi`/`MainActivity`：`KEY_ONLINE_LOCATION`（默认开）+ 开关接线（“地点编码在线解析：离线库查不到时才联网解析一次”）
  - 单测：`AdcodeResolverTest` 7/7（归一化/精确/回退/未知/ip33解析/去重）；`PublishInfoTest` +3（解码/未知留原文/POI优先）；全量 123/123
- 核心逻辑：离线优先、在线仅在离线 miss 时触发且可关闭；POI 名永远优先（真名不解析）；解析失败永不伪造（显示原文或空）
- 风险点：低。离线表覆盖全部国标编码，在线失败静默；最坏退化为显示原始编码（即现状）
- 真机验证：
  - [ ] LSPosed：原来显示数字编码/空白的视频现显示“省 市 区”；POI 视频仍显示 POI 名
  - [ ] 离线库 miss 的编码：联网后约1秒内从编码变为地名（`[Adcode] online resolved` 日志）
  - [ ] 关闭“地点编码在线解析”后：未知编码保持原文，不再联网（无 `[Adcode]` 日志）
- 备注：区划数据源 modood/Administrative-divisions-of-China（WTFPL，已核对，生成文件头注明来源）；在线接口与 FreedomPlus 同源（仅借用公开接口地址与“city 是编码”的字段语义，未复制其源码）

## 修复：主动暂停后不再被过滤划走（2026-09-23）

- commit：`fix: keep user-paused item from auto filter`
- 现象（用户真机反馈）：图文推荐点暂停想看一下，暂停了还是被划走
- 根因：`moveToNext` 仅有两个调用方——播放完成/错误/循环边界三路都查 `userPaused` 并避让，唯独过滤分支（`checkCurrentFeedContent→filterCurrentItem`）从不看暂停状态，3 次确认后照样划走
- 修改文件：
  - `hook/ImmersiveUi.java`（修改）：过滤原因成立后先过暂停保护分支（仿直播 tab 豁免写法：清候选 + `[Filter] keep: user paused aid=` 按 aid 限流记一条 + 返回 false 让扫描继续，overlay 照常显示）；新增 `lastUserPausedKeepAid` 限流字段
  - `hook/FeedNavigator.java`（修改）：新增纯判定 `shouldKeepForUserPause(reason, userPaused)`（放 FeedNavigator 而非 ImmersiveUi：后者静态 Handler 导致 JVM 单测类初始化崩溃，导航判定归一处也可测）
  - 单测：`FeedNavigatorTest` +3（暂停+任意原因保留/播放中不过滤保护/无原因不保护）；全量 126/126
- 核心逻辑：暂停保护适用于全部过滤原因（与完成/错误自动下一条一致）；切内容时 `clearUserPauseForContentChange` 先清旧暂停（aid 已知且变化），手动上滑经 `confirmUserFeedSwitch→userPlaying()` 清暂停，所以保护不会漏到下一条；恢复播放后需重新攒 3 次确认才过滤（约 660ms 缓冲，不会秒划）
- 风险点：极低。纯加保护分支；暂停语义本身无改动
- 真机验证：
  - [ ] LSPosed：图文/广告/直播出现后点暂停，停住不再自动划走，日志有 `[Filter] keep: user paused`
  - [ ] 暂停后手动上滑：下一条过滤行为正常（暂停不泄漏）
  - [ ] 暂停后点恢复：约 1 秒内不秒划（需重新确认）

## 诊断：进度条偶发缺失加 near-miss 日志（2026-09-23，未修复，先取证）

- commit：`diag: log progress-bar near misses`
- 现象（用户真机反馈）：有些视频下面不显示进度条；点暂停后进度条出现；普通视频也偶发
- 已排除：`hide()` 只改 alpha 不改 visibility，收集每轮重跑——不存在“一次 miss 永久隐藏”自锁。剩余两种可能：①分类器没认出（类名/几何阈值）；②抖音原生自动隐藏（暂停时宿主自己恢复 UI，所以“暂停出现”不能区分两者）
- 修改文件：
  - `hook/ProgressBarClassifier.java`（修改）：判定拆为 `matchesProgressSize`/`isBottomStrip`（`isProgressBar` 语义零改动）；新增纯函数 `rejectStage`（返回首个失败环节 class/decor/width/height/top）
  - `hook/ImmersiveUi.java`（修改）：`collectProgressViews` 对类名命中但几何失败的记 `[FeedUi] progress reject 类名 w/h/top stage=`；对几何像进度条但类名不命中的记 `[FeedUi] progress-like unknown class=`；各按 view 限流一条。非候选 view 先过免费宽高预检才调 `getLocationOnScreen`，无额外性能开销
  - 单测：`ProgressBarClassifierTest` +2（拆分判定一致/rejectStage 各环节）；全量 128/128
- 核心逻辑：本包不改变任何显示行为，只加诊断；拿到日志后按数据修阈值/类名（或确认原生自动隐藏后做强制显示）
- 风险点：无（行为零改动；日志按 view 限流）
- 真机验证（请用户配合）：
  - [ ] 刷到没进度条的视频后进插件导出日志，把含 `[FeedUi]` 的行发回（重点看 `progress reject` 的 stage 和 `progress-like unknown` 的类名）
  - [ ] 同时备注那条是普通视频/长视频/合集/广告中的哪种

## 修复：长视频阈值输入框 hint/键盘错误（2026-09-23）

- commit：`fix: correct threshold input hint and keyboard`
- 现象（用户反馈）：填秒数的输入框 hint 显示 `#FFFFFF`
- 根因：`addLabeledField` 写死颜色 hint 与文本键盘，阈值框复用时没改
- 修改文件：
  - `MainActivity.java`（修改）：`addLabeledField` 增加 `hint`/`inputType` 参数；阈值框 hint 改“例如：180”+ 数字键盘，三个颜色框保持 `#FFFFFF` + 文本键盘
  - 单测：无（纯 Activity glue）；全量 128/128
- 核心逻辑：仅 UI 文案与键盘类型；阈值校验（5~7200 秒）与毫秒换算不动
- 风险点：无
- 真机验证：
  - [ ] 阈值框清空后 hint 显示“例如：180”，弹出数字键盘；颜色框 hint 仍是 `#FFFFFF`

## 功能：圆形取色器弹窗（2026-09-23）

- commit：`feat: circular color picker dialog`
- 起因（用户要求）：颜色不要手填 hex，弹窗圆形取色盘，手指按压滑动选择
- 修改文件：
  - `ColorMath.java`（新增，纯逻辑）：HSV↔RGB 换算 + `toHex`（不透明 `#RRGGBB`，半透明 `#AARRGGBB`，与 `FeedUiStyle` 解析格式对齐）；无 android 依赖以便 JVM 单测
  - `ColorWheelView.java`（新增，自绘 View）：外圈 SweepGradient 选色相，内方块横向饱和度/纵向明度，按压滑动实时取色；圆心整片内盘都响应（角落钳制）；补 `performClick` 消 ClickableViewAccessibility 警告
  - `ColorPickerDialog.java`（新增）：深色弹窗，色轮 + 不透明度 SeekBar + 实时预览色块/hex；确定回传 ARGB，取消丢弃
  - `MainActivity.java`（修改）：三个颜色行右侧加 48dp 预览色块，点色块弹窗（初值取当前框内颜色）；确定后回填输入框 + 刷新色块 + 直接走原 `saveColorSettings` 保存；输入框样式抽 `styleTextInput` 复用（阈值框行为不变）
  - 单测：`ColorMathTest` 7/7（六原色/灰度/回环/包透明度/toHex）；全量 135/135
- 核心逻辑：hex 输入框保留（仍可手填）；取色器只是另一条输入路径，保存/校验/离线同步全走原子流程；透明度经 8 位 hex 往返（`parseColor` 原生支持）
- 风险点：低。新增 UI 不碰 hook 逻辑；最坏取色器布局在小屏上拥挤（内容约 560dp 高）
- 真机验证：
  - [ ] 点任一颜色行右侧色块弹窗，色轮滑动选色 + 透明度条，预览/hex 实时变，确定后输入框/色块/实际文字颜色都变
  - [ ] 手填 hex + 保存颜色老流程仍可用

## 修复：Android 11+ 日志不可见（所有文件访问权限）（2026-09-23）

- commit：`fix: request all-files access for log viewer`
- 现象（用户反馈）：插件里完全看不到日志
- 根因：日志写在抖音的 `Android/media/<抖音包名>/` 下（抖音进程写自家目录，免权限，写侧正常）；但读侧（我们 App 进程）在 Android 11+ 上即使有 READ 权限也进不了别家 App 的 media 目录——之前只申请 READ，11+ 上必然空列表（13+ 上 READ 还直接不授）。与 Sesame-M 同构，照搬它的解法
- 修改文件：
  - `AndroidManifest.xml`（修改）：新增 `MANAGE_EXTERNAL_STORAGE`；补 `WRITE_EXTERNAL_STORAGE`（maxSdk 29，供 28/29 删除日志用）
  - `LogViewerActivity.java`（修改）：30+ 查 `isExternalStorageManager`，没有就跳“管理所有文件”设置页（先本应用页，OEM 缺失则回退总表，再缺 Toast 指引手动开）；28/29 改申 READ+WRITE 双权限；`onResume` 统一 reload（从设置页回退自动刷新）；不可读提示文案按版本区分；删 `< M` 死分支（minSdk 28，顺手消 ObsoleteSdkInt）
  - 单测：无（纯 Activity glue）；全量 135/135
- 核心逻辑：写侧不动；只修读侧权限。用户只需开一次开关
- 风险点：低。权限页跳转三级回退；OEM 无该页面时 Toast 指引
- 真机验证：
  - [ ] Android 11+：进运行日志点授权按钮，跳设置开“管理所有文件”，回退后文件列表出现
  - [ ] 28/29：弹窗申请存储权限后列表出现；清空按钮可用

## 调整：取色器改实心圆盘（2026-09-23）

- commit：`feat: solid-disc color picker`
- 起因（用户要求）：不要方块加圆环，直接给实心圆
- 修改文件：
  - `ColorWheelView.java`（重写）：实心圆盘按像素绘制（角度=色相，半径=饱和度，圆心白）；触摸钳制到盘内；明度移出 view，由外部 `setValue` 传入
  - `ColorPickerDialog.java`（修改）：加“明度”滑条（0~100，初值取当前色明度）；圆盘+明度+不透明度三路实时联动预览/hex
  - 单测：无新增（`ColorMath` 未动）；全量 135/135
- 核心逻辑：圆盘只管色相/饱和度，黑/灰/深色靠明度条（拉到 0 即黑）；其余流程（色块入口/回填/保存）不变
- 风险点：极低。圆盘位图按尺寸一次性生成（约 60 万像素，常量开销）
- 真机验证：
  - [ ] 弹窗为实心圆盘，点/拖选色，标记点跟随；明度拉低颜色变深，拉到 0 为黑色
  - [ ] 确定后输入框/色块/实际文字颜色一致

## 修复：授权按钮被“暂无日志”分支吞掉（2026-09-23）

- commit：`fix: show permission prompt before file probes`
- 现象（用户反馈）：打开插件没有任何 SD 卡权限申请提示
- 根因：Android 11+ 上无 all-files 权限时，别家 media 目录对 `File.exists()` 直接返回 false——`diagnoseEmpty` 先查文件后查权限，落进“暂无日志：抖音进程尚未写入”分支，把授权按钮设了 GONE。用户看到的就是一行字、没有按钮
- 修改文件：
  - `LogViewerActivity.java`（修改）：新增 `needsFilePermission()`（30+ 查 `isExternalStorageManager`，28/29 查 READ 授权），`diagnoseEmpty` 先查权限再探文件；无权限时按版本显示引导文案 + 授权按钮；已有权限仍不可读才提示 adb（按钮隐藏，因为再点也没用）
  - 单测：无（纯 Activity glue）；全量 135/135
- 核心逻辑：权限状态优先于文件探测；`onResume` 回退刷新逻辑不变
- 风险点：无
- 真机验证：
  - [ ] 未授权进运行日志：看到引导文案 + 授权按钮，点后跳设置，开完回退列表出现

## 修复：日志页操作按钮被挤出可视区（2026-09-23）

- commit：`fix: keep log viewer actions out of scroll`
- 现象（用户反馈）：权限按钮和内容把刷新/分享/清空按钮挡住了
- 根因：文件列表、级别过滤、操作行、授权按钮全塞在一个小 ScrollView 里，文件一多操作行就滚出可视区
- 修改文件：
  - `LogViewerActivity.java`（修改）：只文件列表和日志正文各自滚动；标题/级别/刷新分享清空/授权按钮全部固定在位
  - 单测：无（纯布局）；全量 135/135
- 核心逻辑：纯布局调整，零行为改动
- 风险点：无
- 真机验证：
  - [ ] 日志文件多时，刷新/分享/清空/授权按钮始终可见；文件列表内部可滚

## 修复：进度条被宿主原生隐藏后强制显示（2026-09-23）

- commit：`fix: force natively-hidden progress bar visible`
- 现象（用户真机日志）：同一会话同开关，前两条视频进度条正常，第三条（16s 短视频）约 50 轮扫描零 `[FeedUi]` 行——无命中也无 miss，说明该条的进度 view 不可见（宿主 GONE/淡出），只做“免于我们隐藏”不够
- 修改文件：
  - `hook/ProgressBarClassifier.java`（修改）：新增纯判定 `shouldForceVisible`（两 aid 已知且相等才强制，防止回收的旧条泄漏到直播/广告页）
  - `hook/ImmersiveUi.java`（修改）：命中时记录 view+aid（`lastProgressView/lastProgressAid`），`checkCurrentFeedContent` 顺手记当前 aid；每轮先 `keepForcedProgressView`（把强制 view 加入 preserved，保祖先路径但兄弟照藏），藏完再 `forceProgressVisible`（VISIBLE + alpha 1，限流记 `[FeedUi] force progress visible aid=`）；另补宿主隐藏候选诊断 `[FeedUi] progress gone class=`（区分原生隐藏与分类器 miss）
  - 单测：`ProgressBarClassifierTest` +1（aid 门控 7 断言）；全量 136/136
- 核心逻辑：强制只发生在同 aid + view 仍 attached + 常显开关开时；暂停/切内容自动停（暂停不进隐藏块，切内容 aid 不等）
- 风险点：低。最坏是跨内容复用 holder 的 900ms 窗口内（内容检查间隔）旧条多显示一瞬；直播等无条页因 aid 不等不会被强制点亮
- 真机验证：
  - [ ] 那条 16s 视频底部进度条常显；日志出现 `[FeedUi] force progress visible`
  - [ ] 切直播/广告页不出现冻结的旧进度条；暂停/恢复行为不变

## 调整：日志页文件列表自适应高度（2026-09-23）

- commit：`fix: compact log viewer layout`
- 现象（用户反馈）：日志文件和级别过滤中间隔太大，底部正文区太小
- 根因：文件列表 ScrollView 写死 weight 占 1/3 高度，文件少时全是空白；各处边距也偏大
- 修改文件：
  - `LogViewerActivity.java`（修改）：文件列表改自适应高度（封顶 180dp，超了内部滚）；根 padding 20/32→16，各区间距收紧到 6~8dp；正文区独占剩余全部高度
  - 单测：无（纯布局）；全量 136/136
- 核心逻辑：纯布局调整，零行为改动
- 风险点：无
- 真机验证：
  - [ ] 文件少时级别过滤紧跟文件列表，无大片空白；正文区明显变大

## 修复：日志页标题顶进状态栏（2026-09-23）

- commit：`fix: pad log viewer below system bars`
- 现象（用户反馈）：运行日志几个字跑到状态栏上面去
- 根因：targetSdk 35 强制 edge-to-edge，内容从屏幕顶排；之前靠 32dp 顶部边距硬躲，上次收紧到 16dp 后盖不住状态栏高度
- 修改文件：
  - `LogViewerActivity.java`（修改）：根布局加 `OnApplyWindowInsetsListener`，上下 padding 叠加真实系统 insets（状态栏/导航栏/挖孔），不再猜高度
  - 单测：无（纯布局）；全量 136/136
- 核心逻辑：纯布局；minSdk 28 原生支持该 API，无需 androidx
- 风险点：无
- 真机验证：
  - [ ] 标题与状态栏拉开距离，底部按钮不被导航条遮挡

## 功能：版本号+编译时间三处对齐（2026-09-23）

- commit：`feat: show version and build time in UI and logs`
- 起因（用户要求）：首页标题右边加版本和编译时间，抖音加载成功日志也打印，方便确认版本（LSPatch 下管理界面与补丁包内 hook 可能是两个版本，必须能一眼区分）
- 修改文件：
  - `app/build.gradle.kts`（修改）：`buildFeatures.buildConfig=true`；`BUILD_TIME` 字段；时间戳抽 `buildTimestamp` 单一来源，APK 归档文件名共用——界面/日志/APK 三处时间戳完全一致
  - `ModuleVersion.java`（新增，纯逻辑）+ `ModuleVersionTest` 2/2：统一 `v1.5.1+17 20260923_234853` 格式
  - `MainActivity.java`（修改）：标题行右侧加小字版本号
  - `hook/DouyinModule.java`（修改）：`loaded in` 与 `hook installation finished` 追加 `module=v...+...`
  - 单测：全量 138/138
- 核心逻辑：管理界面显示的是管理 APK 的版本；日志里的是补丁包内 hook 的版本——两者时间戳一致才说明补丁打的是最新包
- 风险点：无
- 真机验证：
  - [ ] 首页标题右侧显示版本；重打补丁后日志 `hook installation finished` 行 module 时间戳与管理界面一致

## 修复：发布按钮隐藏持续强制+miss 诊断（2026-09-23）

- commit：`fix: enforce publish hide and log near misses`
- 现象（用户反馈）：底部发布 + 按钮隐藏没效果（之前日志证明藏过一个 LinearLayout，但按钮仍在）
- 根因（双假设）：①藏错 view（真按钮容器宽约 20% 超出 18% 上限，或内部图标无 clickable/描述信号）；②藏后被宿主恢复（旧逻辑每 view 只藏一次，恢复后永不再藏）
- 修改文件：
  - `hook/FeedUiHider.java`（修改）：判定拆 `isPublishGeometry`/`isPublishSignal`（语义不变）；`hideInto` 改持续强制（匹配且非 GONE 就重藏，复藏日志 5 秒限流 `[FeedUi] re-hide`）；miss 分支记 `[FeedUi] publish miss 类名 geo/sig/几何/clickable/描述`（每 view 一次，geo/sig 全无的跳过防刷屏）
  - 单测：`FeedUiHiderTest` +1（拆分一致）；全量 139/139
- 核心逻辑：强制只发生在“仍匹配”的 view 上，回收复用后不匹配的不碰；TAB 共用同一强制逻辑
- 风险点：低。若真因是①，需按 miss 日志再调阈值/信号（本包先取证）
- 真机验证（需重打补丁）：
  - [ ] 开隐藏后 + 按钮消失且不再回来；日志有 `hide publish`（若曾恢复则有 `re-hide`）
  - [ ] 若仍在：贴 `publish miss` 行，我按真实几何调阈值

## 修复：发布按钮按描述锚定藏整个 tab 项（2026-09-24）

- commit：`fix: hide publish tab item by description anchor`
- 起因（用户要求）：参考开源实现与 DYHelper；之前只藏命中的小 view，真按钮还在
- 开源结论：
  - FreedomPlus：DexKit 定位 MainBottomTabView 容器 → 子 view 按 contentDescription 关键词匹配 → 藏 X-前缀 tab 项祖先（不是 glyph 自己），无几何
  - DYHelper：DexKit `publish_button_classes` 直解发布键类 + BottomBar 管理器（实现侧字符串加密，只确认路线）
- 修改文件：
  - `hook/FeedUiHider.java`（修改）：新增描述锚定路径——底部中央（top≥85%/横向±20%）contentDesc 含“发布”的 view，向上走到最大仍符合 tab 项尺寸（宽≤30%/高≤20%）的祖先，整个藏掉（DexKit-free 等价实现）；走不通记 `[FeedUi] publish desc-miss stage=area/size`；原小图标路径保留
  - 单测：`FeedUiHiderTest` +2（tab 项尺寸/描述区域）；全量 141/141
- 核心逻辑：GONE 整个 tab 项（邻 tab 回流，与顶部 TAB 隐藏一致）；强制与恢复走原 map；评论区等处的“发布”按钮因不在底部中央区不会被误伤
- 风险点：低。若某版本 + 键无“发布”描述，则退化为旧行为（等 miss 日志再加信号）
- 真机验证（需重打补丁）：
  - [ ] 开隐藏后底部 + 整个 tab 位消失（邻 tab 展开）；日志 `hide publish-tab view=...`
  - [ ] 评论区发布按钮不受影响；关开关后 + 回来
